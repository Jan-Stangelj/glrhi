#version 460 core

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout(binding = 0, rgba16f) uniform image3D voxelOut;

void main() {
    imageStore(voxelOut, ivec3(gl_GlobalInvocationID.xyz), vec4(0.0));
}
